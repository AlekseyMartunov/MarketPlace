import React, {useState} from 'react';

const Login = () => {
    const [price, setPrice] = useState("Price")
    return (
        <div>
           login
        </div>
    );
};

export default Login;